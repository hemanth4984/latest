
version: 0.2
phases:
  install:
    commands:
      - npm install 
      - aws cloudformation package --template-file api.json  --s3-bucket hemanth99
                                   --output-template-file master.json
artifacts:
  type: zip
  files:
    - api.json
    - master.json
